apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-overview-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  api-gateway-overview.json: |-
    {
      "annotations": {"list": []},
      "editable": true,
      "fiscalYearStartMonth": 0,
      "graphTooltip": 0,
      "id": null,
      "links": [],
      "liveNow": false,
      "panels": [
        {
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "fieldConfig": {"defaults": {"unit": "percentunit"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
          "id": 1,
          "title": "CPU usage (cores)",
          "type": "timeseries",
          "targets": [
            {
              "expr": "sum(rate(container_cpu_usage_seconds_total{pod=~\"api-gateway.*\", container!=\"\"}[5m]))",
              "refId": "A",
              "legendFormat": "cpu cores"
            }
          ]
        },
        {
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "fieldConfig": {"defaults": {"unit": "bytes"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
          "id": 2,
          "title": "Memory usage",
          "type": "timeseries",
          "targets": [
            {
              "expr": "sum(container_memory_working_set_bytes{pod=~\"api-gateway.*\", container!=\"\"})",
              "refId": "A",
              "legendFormat": "memory bytes"
            }
          ]
        },
        {
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
          "id": 3,
          "title": "Pod restarts",
          "type": "timeseries",
          "targets": [
            {
              "expr": "sum by (pod) (kube_pod_container_status_restarts_total{pod=~\"api-gateway.*\"})",
              "refId": "A"
            }
          ]
        },
        {
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
          "id": 4,
          "title": "Pod ready replicas",
          "type": "stat",
          "targets": [
            {
              "expr": "sum(kube_pod_status_ready{pod=~\"api-gateway.*\", condition=\"true\"})",
              "refId": "A"
            }
          ]
        }
      ],
      "refresh": "30s",
      "schemaVersion": 38,
      "tags": ["sre-platform", "api-gateway"],
      "templating": {"list": []},
      "time": {"from": "now-1h", "to": "now"},
      "timezone": "",
      "title": "api-gateway-overview",
      "uid": "api-gateway-overview",
      "version": 1,
      "weekStart": ""
    }
