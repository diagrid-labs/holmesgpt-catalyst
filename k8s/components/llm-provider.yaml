apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: llm-provider
spec:
  type: conversation.openai
  version: v1
  metadata:
    - name: key
      secretKeyRef:
        name: sre-agent-secrets
        key: OPENAI_API_KEY
    - name: model
      value: "gpt-4o-mini"
